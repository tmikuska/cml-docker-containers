# README

The build script checks online what's the latest available package is that
Google provides and uses that. This is done via the `get_latest_version.sh`
script which is called in the `vars.mk` sub-makefile.
